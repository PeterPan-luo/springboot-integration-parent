
mybatis:
    type-aliases-package: com.luo.study.mmmapper.model
    mapper-locations: classpath:mapper/*.xml

mapper:
#    mappers:
#        - tk.mybatis.mapper.common.Mapper -- 自定义的mapper
    not-empty: false
    identity: ORACLE

pagehelper:
    helperDialect: oracle
    reasonable: true
    supportMethodsArguments: true
    params: count=countSql

spring:
  profiles:
    active: dev
